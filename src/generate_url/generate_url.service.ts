const data = {
    status: true,
    day: "today"
}

class GenerateUrlService{
    public generateUrl(query: any){

        return query
    }

    public makeUrl(url: string, query: any): string{
        return url
    }
}

export default GenerateUrlService